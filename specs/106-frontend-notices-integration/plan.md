# Implementation Plan: Frontend Notice Boards

**Branch**: `106-frontend-notices-integration` | **Date**: 2026-06-02 | **Spec**: `/specs/106-frontend-notices-integration/spec.md`
**Input**: Feature specification from `/specs/106-frontend-notices-integration/spec.md`

## Summary
Build the frontend interfaces for role-specific Notice Boards and integrate a global Notification Dropdown into the main navigation bar. Verify and slightly adjust backend `Notice` models/controllers to support teachers sending notices to admins or specific classes.

## Technical Context

**Language/Version**: TypeScript 5.4+ (Backend), HTML/JS (Frontend)
**Primary Dependencies**: TailwindCSS, FontAwesome, Express
**Target Platform**: Browser
**Project Type**: Fullstack Web Application
**Constraints**: Must use vanilla JS/Web Components on the frontend as per existing project standards (`ui-components.ts`).

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] Smallest viable diff: Reusing existing HTML structures and just injecting the API logic.
- [x] No unrelated refactoring: Only touching notice-related UI and necessary backend tweaks.
- [x] Test-first: Integration tests exist for the backend. We will rely on manual DOM verification for the frontend components.

## Project Structure

### Documentation (this feature)

```text
specs/106-frontend-notices-integration/
├── plan.md              # This file
├── research.md          # Implementation details and backend gaps
└── tasks.md             # (To be generated by /sp.tasks)
```

### Source Code (repository root)

```text
public/components/
├── ui-components.ts     # UPDATE: Add Notification Bell & Dropdown
public/protected/admin/
├── notices.html         # CREATE/UPDATE: Admin Notice Board
public/protected/teacher/
├── notices.html         # CREATE/UPDATE: Teacher Notice Board
public/protected/student/
├── notices.html         # UPDATE: Student Notice Board (view only)
public/protected/parent/
├── notices.html         # UPDATE: Parent Notice Board (view only)
schemas/models/
├── notice.model.ts      # UPDATE: Check `audience` enum
controllers/
├── teacher.controller.ts# UPDATE: Add Notice creation for teachers
routes/
├── teacher.routes.ts    # UPDATE: Add notice routes
```

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| Duplicate HTML pages for Notices | Keeps role logic isolated | A single dynamic page requires heavy JS routing and complex conditional rendering, violating the "keep it simple" vanilla JS approach used so far. |
