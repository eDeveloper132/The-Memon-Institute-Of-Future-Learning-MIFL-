# Routes

This directory defines the API endpoints and maps them to their respective controller methods.

## Structure

- Group routes by feature (e.g., `auth.mts`, `user.mts`).
- Use Express Router for modularity.
